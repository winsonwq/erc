@import "../../lib/*.styl"

* {
  box-sizing: border-box;
}

body {
  margin: 0;
}

#app {
  padding: 10px;

  p {
    margin: 0 0 10px 0;
  }
}

.styled-label {
  text-align: left;
  display: inline-block;
  padding: 10px 0;
  min-width: 100px;
}

.label-editor {
  resize: none;
  padding: 5px;
  border: 1px solid #ccc;
  outline: none;
}
